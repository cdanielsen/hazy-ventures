import React, { Component } from 'react';

export default class Questionare extends Component {
    render() {
      return (
        <div>Questionaire Goes Here
        </div>
      );
    }
}